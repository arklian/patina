services:
  app:
    image: patina-test
    platform: linux/amd64
    ports:
      - "8080:8080"
    environment:
      - DATABASE_HOST
      - DATABASE_PORT
      - DATABASE_DB
      - DATABASE_USER
      - DATABASE_PASSWORD
