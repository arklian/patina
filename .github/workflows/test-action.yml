name: test-action
run-name: ${{ github.actor }} is testing stuff
on:
  workflow_dispatch:
    inputs:
      testInput:
        description: 'a test input'
        required: true
        default: 'ooo'
      branch:
        description: 'test inpu'
        required: true
        default: 'main''

jobs:
  testing:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "TestInput: ${{ github.event.inputs.testInput }}"
          echo "branch input: ${{ github.event.inputs.branch }}"

